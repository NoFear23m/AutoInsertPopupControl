<Window x:Class="IntelliSenseTestwindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestApp"
        xmlns:AutoInsertPopup="clr-namespace:AutoInsertPopup;assembly=AutoInsertPopup"
        mc:Ignorable="d"
        Title="IntelliSenseTestwindow" Height="450" Width="800">
    <Grid Margin="10">
        <StackPanel HorizontalAlignment="Left">
            <TextBox x:Name="txtTest1" Width="300"/>
        </StackPanel>
        <AutoInsertPopup:AutoInsertPopupControl TargetControl="{Binding ElementName=txtTest1}" HorizontalAlignment="Left"
                                                 VerticalAlignment="Top" ItemSelectedCommand="{Binding SelectItemCommand}" OpenOnTargetFocused="True"
                                                x:Name="pop1" AutoInsertList="{Binding TestList}" Popupanimation="Fade" FocusKey="Down" OpenPopupTriggerChar=". (" ReplaceTriggerChar="False"
                                                StaysOpen="False" BorderBrush="Black" BorderThickness="1" FilterStrategy="StartsWith" MaxHeight="80"
                                                OverridePlacementWithCursorPosition="True"/>
    </Grid>
</Window>
